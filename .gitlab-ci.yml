pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r . public
    - rm -rf public/.git public/.gitlab-ci.yml public/.gitlab public/.gitignore
  artifacts:
    paths:
      - public
  only:
    - main