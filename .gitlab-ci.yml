pages:
  stage: deploy
  script:
    - echo "Listing public/"
    - ls -la public || true
    - head -n 20 public/index.html || true
  artifacts:
    paths:
      - public
  only:
    - main